<div class="wb-logo-pop">
    {% if board.template.logo %}
        <a  href="{{ path('show_website',{'city':board.locality.slugcity,'nameboard':board.slug}) }}" class="wb_logo" style="background-image: url('{{ asset('spaceweb/template/'~board.template.logo.namefile ) }}')">
        </a>
    {% else %}
        <a href="{{ path('show_website',{'city':board.locality.slugcity,'nameboard':board.slug}) }}" class="wb_logo" style="
                background-image: url('{{ asset('img/pinsaffi.png') }}')">
        </a>
    {% endif %}
</div>
