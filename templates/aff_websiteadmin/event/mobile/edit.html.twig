{% block body %}
    <section class="_wb-ospace_mob">
        <x-event board="{{ board.id }}"  partners="{{ partners|serialize(format = 'json') }}" event="{{event|serialize(format = 'json') }}" city="{{ city }}"></x-event>
    </section>
{% endblock %}
{% use "js/js-event.html.twig" %}
{% block javascripts %}
    {{ parent() }}
{% endblock %}

