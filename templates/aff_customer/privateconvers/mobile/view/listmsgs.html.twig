{# list-message version mobile #}
<div class="wb_header centered">
    <div id="id-choicacces">
        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
            {%  if msgs is defined %}
                {% for msg in msgs %}
                    {% apply spaceless %}
                    <a class="Ax {{ msg.sender ?"":"Ax-no"}}" href="{{ path('read_private_convers_dp',{'id':msg.id})}}">
                        <div class="_gr_row_mb  inf-msg {{ msg.sender ?" _co-gy gxcsal":""}}">
                            <div class="left_msg_dt_mb">
                                <div class="_bb-msg {{ msg.sender ?"_bg-Wfilt":"_bg-R"}}"></div>
                                <div class="dt_mb-msg">
                                    {% if msg.createAt|format_datetime('short', 'none', locale='fr') is same as ("now"|format_datetime('short', 'none', locale='fr')) %}
                                        {{ msg.createAt|format_datetime('none', 'short', locale='fr') }}
                                    {% else %}
                                        {{ msg.createAt|format_datetime('medium', 'none', locale='fr')|slice(0,-4) }}
                                    {% endif %}
                                </div>
                            </div>
                            <div class="_fx-C_mb fx-cst">
                            <div class="_gr-Ax_mb">
                                <div class="{{ msg.sender ?"_co-gyb":"_co-bk"}}">{{- msg.dispatchopen.name|trim -}}</div>
                            </div>
                                <div class="_co-gyBd">
                                    {{- msg.subject|u.truncate(20, '...', false) -}}
                                </div>
                            </div>
                            {% if msg.pj is defined %}
                            <div class="andel">
                                <div><p>pièce jointe</p></div>
                            </div>
                            {% endif %}
                        </div>

                    </a>
                    {% endapply %}
                {% endfor %}
                <div class="men_col_mb men_col_mb">
                    {% if pagination is not null %}
                        <div class="bta-lk_v2" id="mormsg">
                            {{ knp_pagination_render(pagination) }}
                        </div>
                    {%  endif %}
               </div>
            {% endif %}
        {% endif %}
    </div>
</div>