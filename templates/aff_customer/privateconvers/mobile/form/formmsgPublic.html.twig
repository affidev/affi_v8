<div class="msg-memberwebsite_mb">
            <h2>Démarrez une conversation avec {{ website.namewebsite }}</h2>
        </div>
        {{ form_start(form) }}
        {{ form_errors(form) }}
        <div class="bg-glass-W">
            <div class="intro_form">
                <div class="formmsg">
               {#    {{ form_label(form.subject, "objet:") }}
                    {{ form_widget(form.subject) }}#}
                    {{ form_label(form.content, "votre message :") }}
                    {{ form_widget(form.content) }}
                    <div class="validate"></div>
                    <div class="form-send">
                        {#    <div class="g-recaptcha" data-sitekey="6LfPKvAUAAAAAIopX1KBsmBtSrI8FekLD-u-s4N5"></div> #}
                        <i class="fa fa-telegram" id="postform"></i>
                    </div>
                </div>
            </div>
        </div>
        {{ form_rest(form) }}
        <div class="loading"></div>
        <div class="error-message"></div>
        {{ form_end(form) }}
    </div>
</div>

<div class="wait">
    <i class="fa fa-spinner fa-spin"></i>
</div>